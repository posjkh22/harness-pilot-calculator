# URS-001 SW 요구사항 명세 (SRS)

| 항목 | 값 |
| --- | --- |
| URS ID | URS-001 |
| 제목 | 기술 기반 |
| 생성 시각 | 2026-09-15T12:23:28.742Z |
| 영향 영역 | electron/main, electron/preload, renderer/ui, build, test-infra |

## 1. 원본 사용자 요구사항 요약

Node.js, Electron, HTML, CSS를 사용해 애플리케이션을 구현한다.

| AC ID | Given | When | Then |
| --- | --- | --- | --- |
| AC-URS-001-01 | 앱 구현물이 준비되어 있다. | 구현에 사용된 기술 기반을 확인한다. | Node.js, Electron, HTML, CSS가 모두 사용되어 있어야 한다. |

## 2. 기술 명세

### SRS-URS-001-01 (build)

**명세**: 애플리케이션은 Node.js 실행 환경과 Electron 런타임을 사용하고, UI는 HTML·CSS·JavaScript로 구성해야 한다.

**근거**: URS-001의 기술 기반 제약을 충족한다.

| 항목 | 값 |
| --- | --- |
| 검증 대상 AC | AC-URS-001-01 |
| 검증 전략 | ELECTRON_INTEGRATION |
| 변경 예상 파일 | package.json, electron/main.js, electron/preload.js, ui/index.html, ui/calculator.js, ui/styles.css |
| 테스트 훅 | package.json 실행 스크립트 확인, Electron 창에서 HTML·CSS 로드 여부 확인 |

**인터페이스 계약**

| 유형 | 이름 | 방향 | 요청 | 응답 | 오류 | 권한 |
| --- | --- | --- | --- | --- | --- | --- |
| MODULE | electron/main.js | INTERNAL | Electron 앱 시작 이벤트 | BrowserWindow 생성 및 ui/index.html 로드 | 창 생성 실패 시 오류 로그 및 프로세스 종료 처리 | OS 창 생성 권한 |
| MODULE | ui/index.html | INTERNAL | 정적 HTML 문서 | 계산기 UI DOM | 문서 로드 실패 | 브라우저 렌더러 권한 |

**데이터 모델**

- 런타임: Node.js 및 Electron
- 표현 계층: HTML DOM과 CSS 스타일
- 동작 계층: renderer의 일반 JavaScript 모듈

**오류 처리**

- 빌드·실행 오류는 표준 오류 로그로 기록한다.
- 렌더러 오류는 콘솔에 기록한다.

**보안**

- contextIsolation은 true로 유지한다.
- nodeIntegration은 false로 유지한다.
- sandbox는 true로 설정한다.
- 렌더러에 Node.js API를 직접 노출하지 않는다.

**성능·운영**

- 계산 로직은 로컬 동기 연산으로 처리한다.
- 초기 화면은 단일 정적 HTML/CSS/JS 번들로 로드한다.

## 3. 보안 결정

| 설정 | 값 | 기본값 약화 | 근거 |
| --- | --- | --- | --- |
| contextIsolation | true | 아니오 | Electron 기본 보안 경계를 유지한다. |
| nodeIntegration | false | 아니오 | 렌더러의 Node.js 모듈 접근을 차단한다. |
| sandbox | true | 아니오 | 렌더러 샌드박스를 활성화한다. |

## 4. 마이그레이션·호환성

- 신규 시스템이며 기존 인터페이스·저장 포맷이 없어 마이그레이션 대상이 없다.

## 5. 구현 순서

1. Node.js/Electron package 설정
2. main process 창 생성 구현
3. preload 보안 설정 구현
4. HTML·CSS·renderer JavaScript 구현
5. Electron 실행 및 패키징 검증

## 6. 위험·제외·미결정

**위험**

- Electron 버전과 로컬 Node.js 버전의 호환성
- 패키징 환경별 렌더러 리소스 경로 오류

**제외 범위**

- 특정 운영체제별 설치 프로그램 배포 자동화
- Node.js/Electron 버전의 제품별 세부 고정값

**미결정 사항**

-
